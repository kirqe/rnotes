// import * as types from '../actions/actionTypes's

const initialState = {
  note: {
    id: null,
    body: ""
  }
}

export default function noteForm(state = initialState, action) {
  return state
}
